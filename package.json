{
  "name": "@repo-tests/root",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/plugins/*",
    "packages/bundle"
  ],
  "scripts": {
    "build": "cd packages/bundle && bun run build",
    "dev": "bun test ./tests/test.repo.ts --concurrent --max-concurrency 10",
    "test": "bun test",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "bun-types": "latest",
    "prettier": "^3.6.2",
    "tsdown": "^0.9.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/bun": "^1.3.3"
  }
}
