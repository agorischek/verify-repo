{
  "name": "@repo-tests/root",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/core",
    "packages/plugins/*",
    "packages/bundle"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "bun test ./tests/test.repo.ts --concurrent --max-concurrency 10",
    "test": "bun test"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0",
    "bun-types": "latest",
    "tsdown": "^0.9.0"
  },
  "dependencies": {
    "@types/bun": "^1.3.3"
  }
}
