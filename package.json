{
  "name": "@verify-repo/root",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/engine",
    "packages/plugins/*",
    "packages/bundle"
  ],
  "scripts": {
    "build": "cd packages/bundle && bun run build",
    "dev": "bun packages/bundle/src/run.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "bun-types": "latest",
    "prettier": "^3.6.2",
    "simple-git": "^3.30.0",
    "tsdown": "^0.9.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@types/bun": "^1.3.3"
  }
}
