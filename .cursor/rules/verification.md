# Verification

Before making a change, you must author a `task.verify.ts` file. You can put it in the repo root. Run `bun verify` from the repo root and show that the file you authored fails. Then work on the requested task. You're not done until `bun verify` passes.